import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pokemon-game-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
